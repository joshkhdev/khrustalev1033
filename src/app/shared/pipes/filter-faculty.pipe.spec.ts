import { FilterFacultyPipe } from './filter-faculty.pipe';

describe('FilterFacultyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFacultyPipe();
    expect(pipe).toBeTruthy();
  });
});
